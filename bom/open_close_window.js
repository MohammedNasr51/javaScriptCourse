/*
 * open(url [opt], window name or target attribute [opt], window features [opt], history replace [opt]) 
 * close() - close the current window
 * window features
 * width, height, left, top
 */

open('https://www.google.com', '_blank', 'width=400,height=400,left=100,top=100');

close();