/*
 * BOM Location Object
    * href get / set [url || hash || file || mail]
    * host like www.google.com:80
    * hostname like www.google.com
    * port like 80
    * pathname like /index.html
    * hash
    * protocol
    * reload() - reload the current page
    * replace() - replace the current page with a new one
    * assign() - load a new document
 */
//& get current url
console.log(location.href);