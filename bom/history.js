/*
 * BOM History Object
    * length - number of entries in the history list
    * back() - move back one entry in the history stack
    * forward() - move forward one entry in the history stack
    * go() - move to a specific entry in the history stack
    * pushState() - adds a new entry to the history stack
    * replaceState() - modifies the current history entry   
 */